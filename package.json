{
  "name": "renode_memory_window",
  "version": "1.0.0",
  "main": "src/main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "start": "npm run build && npx electron --trace-warnings dist/main.js",
    "package": "npx electron-builder --config electron-builder.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "electron": "^35.1.3"
  },
  "devDependencies": {
    "electron-builder": "^26.0.12",
    "typescript": "^5.8.2"
  }
}
